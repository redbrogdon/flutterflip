# FlutterFlip Server Package

Pure Dart server-side package running on Firebase Cloud Functions.

## Endpoints

- `/getMove` (GET) - Takes a board string, player, and search depth via query parameters; computes the next move using minimax.
